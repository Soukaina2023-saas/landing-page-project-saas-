# PageGen Pro - منشئ صفحات الهبوط الذكي

## 🚀 نظرة عامة

PageGen Pro هو نظام SaaS متكامل لتوليد صفحات هبوط احترافية باستخدام الذكاء الاصطناعي. يقوم النظام بتحليل صورة المنتج وتوليد صفحة هبوط كاملة مع صور تسويقية مخصصة.

## ✨ المميزات

- **توليد صور حقيقي**: تكامل مع Replicate API (Flux) لتوليد صور احترافية
- **الحفاظ على هوية المنتج**: استخدام صورة المنتج كمرجع للحفاظ على الشكل
- **توليد نصوص ذكي**: استخدام Gemini لتحليل المنتج وتوليد نصوص تسويقية
- **تصميمات متعددة**: قوالب جاهزة بألوان مختلفة
- **معاينة فورية**: معاينة الصفحة على Desktop و Mobile
- **تصدير سهل**: تحميل HTML أو نسخ الكود

## 📁 هيكل المشروع

```
pagegen-pro/
├── index.html          # الواجهة الرئيسية (HTML + CSS + JS)
├── server.js           # خادم Node.js مع API endpoints
├── package.json        # تبعيات المشروع
├── .env.example        # نموذج إعدادات البيئة
└── README.md           # هذا الملف
```

## 🛠️ التثبيت

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. إعداد متغيرات البيئة

```bash
cp .env.example .env
# ثم عدل ملف .env وأضف مفاتيح API
```

### 3. تشغيل الخادم

```bash
npm start
```

### 4. فتح التطبيق

افتح المتصفح على: `http://localhost:3000`

## 🔑 الحصول على مفاتيح API

### Replicate API Token
1. سجل في [replicate.com](https://replicate.com)
2. اذهب إلى [Account Settings > API Tokens](https://replicate.com/account/api-tokens)
3. أنشئ token جديد

### Gemini API Key
1. سجل في [Google AI Studio](https://makersuite.google.com)
2. اذهب إلى [API Keys](https://makersuite.google.com/app/apikey)
3. أنشئ مفتاح جديد

## 📋 كيفية الاستخدام

1. **ارفع صورة المنتج**: اسحب الصورة أو اضغط لاختيار ملف
2. **أدخل تفاصيل المنتج**: الاسم، الفئة، المشكلة، الفائدة، السعر
3. **اختر النبرة واللون**: احترافي/ودود/عاجل + لون المناسب
4. **أدخل مفتاح الأمان**: Engine Key من ملف .env
5. **اضغط "إنشاء"**: انتظر حتى يتم توليد الصفحة
6. **صدر النتيجة**: تحميل HTML أو نسخ الكود

## 🔧 API Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/health` | GET | فحص حالة الخادم |
| `/api/generate-prompts` | POST | توليد أوصاف الصور |
| `/api/remove-background` | POST | إزالة خلفية الصورة |
| `/api/generate-image` | POST | توليد صورة واحدة |
| `/api/generate-batch` | POST | توليد مجموعة صور |
| `/api/package-result` | POST | تجميع النتيجة النهائية |

## 🎨 الأنيميشنز المضمنة

- **Product Float**: حركة عائمة للمنتج في Hero
- **Scroll Reveal**: ظهور تدريجي للعناصر عند التمرير
- **Glow Pulse**: تأثير نبض للأزرار
- **Gradient Animation**: حركة تدرج لوني
- **Hover Effects**: تأثيرات عند المرور على العناصر

## 🛡️ Product Image Preservation

النظام يحافظ على هوية المنتج من خلال:
1. استخدام الصورة الأصلية كـ reference image
2. تمريرها إلى Flux API مع `prompt_strength: 0.15`
3. توليد صور جديدة تحافظ على شكل المنتج الأصلي

## ⚙️ الإعدادات المتاحة

| Variable | Default | Description |
|----------|---------|-------------|
| `BACKGROUND_REMOVAL_ENABLED` | `false` | تفعيل إزالة الخلفية |
| `IMAGE_GENERATION_ENABLED` | `true` | تفعيل توليد الصور |
| `FLUX_MODEL` | `flux-1.1-pro` | نموذج Flux |
| `BIREFNET_MODEL` | `men1scus/birefnet` | نموذج إزالة الخلفية |

## 🐛 استكشاف الأخطاء

### مشكلة: الأزرار لا تعمل
**الحل**: تأكد من تشغيل الخادم (`npm start`) وأن المنفذ 3000 متاح

### مشكلة: فشل توليد الصور
**الحل**: تأكد من:
1. إدخال REPLICATE_API_TOKEN الصحيح
2. إدخال Engine Key الصحيح في الواجهة
3. تفعيل "توليد الصور بالذكاء"

### مشكلة: Gemini لا يعمل
**الحل**: النظام يستخدم توليد محلي كـ fallback تلقائياً

## 📄 الترخيص

MIT License

## 🤝 المساهمة

نرحب بالمساهمات! يرجى فتح issue أو pull request.

---

**ملاحظة**: هذا مشروع تجريبي للأغراض التعليمية. استخدم مفاتيح API بحرص ولا تشاركها علنياً.
